# lmkd properties
vendor_restricted_prop(vendor_lmkd_prop);

# FM Radio app
vendor_public_prop(vendor_fm_radio_app_prop);

# Camera IDs remapping
vendor_internal_prop(vendor_camera_remapid_prop)

# IR
vendor_internal_prop(vendor_ir_prop)
